description = "Sync to the current QuantumSurge state and plan the next implementation step."
prompt = "Please read docs/CONTEXT.md, docs/ROADMAP.md, and docs/REPO_MAP.md. Identify the current milestone, latest handoff context, and next unchecked roadmap task. Summarize where work stopped and produce a short, concrete implementation plan for the next task instead of executing it by default. If the user explicitly asks to continue building in the same turn, execute the planned task using the Vibe Coding rules."
