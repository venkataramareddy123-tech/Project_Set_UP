# /continue

Execute the next uncompleted task in the QuantumSurge V2 Roadmap.

## Workflow

1. Read `docs/CONTEXT.md` and `docs/ROADMAP.md`.
2. Identify the next uncompleted task in the roadmap.
3. Execute the task according to the Vibe Coding framework.
4. Run `ruff` and regenerate the repo map.
5. Log progress in `ROADMAP.md`.
