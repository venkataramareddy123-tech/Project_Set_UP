# /commit

Generate a Conventional Commit message for staged changes and commit them.

## Workflow

1. Run \`git diff --staged\` to understand the changes.
2. Generate a strict Conventional Commit message (e.g., \`feat(core): update database schema\`).
3. Execute \`git commit -m "<message>"\`.
